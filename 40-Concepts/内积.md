---
type: concept
aliases: [内积, 点积, Inner Product, Dot Product]
domain: 数学基础
tags: [concept]
---

# 内积 Dot Product

## 1. 定义（直觉 → 形式）

**直觉**：两个向量的"对齐程度"。方向一致 → 大正数；正交（无关）→ 零；相反 → 负数。机器学习里"相似度"的默认度量。

**形式**：
$$\langle a, b \rangle = a^\top b = \sum_i a_i b_i$$
几何意义：$\|a\|\|b\|\cos\theta$——模长 × 夹角余弦。

## 2. 数学形式

- **柯西-施瓦茨不等式**：$|a^\top b| \le \|a\|\|b\|$——注意力量级的来源（打分不会超模长乘积）
- **旋转不变性**：$\langle R a, R b \rangle = \langle a, b \rangle$（$R$ 正交）——RoPE 相对性的根据（[[40-Concepts/位置编码]]）
- **与范数的关系**：$\|a\|_2^2 = a^\top a$（见 [[40-Concepts/范数]]）
- **矩阵视角**：$QK^\top$ = 所有 query-key 对的内积打包（[[40-Concepts/注意力机制]] 的打分矩阵）

## 3. 为什么 AI 需要它

| 出现场景 | 用法 |
|---|---|
| [[40-Concepts/注意力机制]] | 相似度打分 $QK^\top/\sqrt{d_k}$ |
| CLIP 对比学习 | 图文嵌入内积（线 8 预埋） |
- [[40-Concepts/位置编码]] | RoPE 的相对性证明 |
| 词向量 | cosine 相似度（内积除以模长） |

## 4. 常见误区

- **误区**：内积大 = 相似——还受模长影响（长向量内积天然大）；要纯方向用 cosine
- **误区**：注意力里的 $\sqrt{d_k}$ 缩放"调温度"——是方差归一（点积方差 = $d_k$），与采样温度不同概念
- **误区**：内积可为负所以能表达"反对关系"——softmax 后只保留相对大小

## 5. 相关概念

- [[40-Concepts/范数]]：自身的特例 $a^\top a$
- [[40-Concepts/注意力机制]]：最大用户
- [[40-Concepts/位置编码]]：旋转不变性
