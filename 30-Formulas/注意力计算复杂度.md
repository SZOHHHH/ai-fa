---
type: formula
formula_id: ATTN-COMPLEXITY
aliases: [注意力复杂度, 注意力计算量, Attention Complexity, 二次复杂度]
domain: 架构
tags: [formula]
---

# 注意力计算复杂度

## 1. 标准形式

单层自注意力（序列长 $n$、维度 $d$、头数 $h$、每头维度 $d_k = d/h$）：

| 阶段 | 计算量 | 显存 |
|---|---|---|
| 投影 $Q/K/V$ | $O(n d^2)$ | $O(nd)$ |
| 打分 $QK^\top$ | $O(n^2 d_k) = O(n^2 d / h)$ | $O(n^2)$ **← 瓶颈** |
| softmax | $O(n^2 h)$ | $O(n^2)$ |
| 加权求和 $\cdot V$ | $O(n^2 d_k)$ | $O(nd)$ |
| **合计** | $O(n^2 d + n d^2)$ | $O(n^2 + nd)$ |

**关键结论**：序列长翻倍 → 注意力计算 ×4（平方律）。$n \gg d$ 时平方项主导——长上下文的数学根源。

## 2. 表示对照表

| 表示名 | 复杂度 | 出处 | 说明 |
|---|---|---|---|
| MHA（标准） | $O(n^2 d)$ 时间、$O(n^2)$ 显存 | Transformer 2017 | 基准 |
| 稀疏注意力（Longformer/BigBird） | $O(n w)$，$w$=窗口 | 2020 | 放弃全局视野 |
| 线性注意力（Performer） | $O(n d^2)$ | 2020 | 核近似 softmax |
| FlashAttention | $O(n^2 d)$ 时间不变、显存 $O(n)$ | 2022 | **数学不变，IO 重排**（见 [[30-Formulas/FlashAttention分块计算]]） |
| Ring/序列分块 | 复杂度不变、跨设备 | 2023 | 分布式 |
| SSM（Mamba） | $O(n d)$ 推理、$O(n \log n)$ 训练 | 2023 | 干掉平方律 |

## 3. 直觉解释

- **为什么是平方**：$n$ 个位置两两配对打分 = $n \times n$——"人人对人人"的代价
- **算术强度视角**：prefill（一次算完提示）compute-bound、decode（逐 token）memory-bound——同一公式两个阶段不同瓶颈（KV cache 的意义所在）
- **墙的真实位置**：$n = 100$k 时注意力矩阵 $10^{10}$ 元素 × 2 字节 = 20GB/头——**显存先爆，不是算力先爆**
- 这一条公式就是线 6（长上下文）与线 1 后半（Mamba/SSM）全部动机的数学浓缩

## 4. 出处

| 论文 | 贡献 |
|---|---|
| [[10-Papers/01-架构演进/Attention Is All You Need（Transformer）]] | 复杂度分析原始出处 |
| [[10-Papers/01-架构演进/FlashAttention- Fast and Memory-Efficient Exact Attention with IO-Awareness（FlashAttention）]] | IO 复杂度理论（新维度） |

## 5. 数学概念分解

- [[40-Concepts/注意力机制]]：被度量的对象
- [[40-Concepts/KV缓存]]：推理阶段的显存账本
- [[30-Formulas/注意力核心公式]]：母公式

## 6. 与其他公式的关系

- → **驱动** [[30-Formulas/FlashAttention分块计算]]（不改复杂度、改常数）
- → **驱动** 线 6 全部论文（稀疏/线性/插值）
- → **驱动** [[30-Formulas/状态空间模型方程]]（线性复杂度的替代路线）
- → 关联 [[40-Concepts/KV缓存]]（decode 阶段的显存面）
