---
type: concept
aliases: [能量模型, Energy-Based Model, EBM]
domain: 数学基础
tags: [concept]
---

# 能量模型 Energy-Based Model

## 1. 定义（直觉 → 形式）

**直觉**：给每个"世界状态"打一个能量分，能量低 = 被模型认可。概率 = 能量的玻尔兹曼指数化 + 归一化。像地形图：谷底（低能量）是数据聚集处，模型学的就是这片地形。

**形式**：
$$p_\theta(x) = \frac{1}{Z_\theta} \exp\!\left( -E_\theta(x) \right), \qquad Z_\theta = \int \exp(-E_\theta(x))\, dx$$
- $E_\theta(x)$：能量函数（神经网络，输出标量）
- $Z_\theta$：**配分函数**——全空间积分，高维下无法计算（EBM 最大的坑）

## 2. 数学形式

- **与 Score 的关系**（绕开 $Z$ 的钥匙）：
$$\nabla_x \log p_\theta(x) = -\nabla_x E_\theta(x)$$
$Z$ 与 $x$ 无关，求梯度时被消掉——所以学 [[40-Concepts/Score函数]] 不用管配分函数！
- **训练的三条路**：对比散度（Hinton RBM 时代）、MCMC 负采样、噪声对比估计 NCE——全都在"回避算 $Z$"上做文章
- **与判别式建模对比**：EBM 是"打分器"不是"生成器"——直到朗之万/SDE 采样把它变成生成模型

## 教程：配分函数从梯度里消失（一个高斯的全程）

**第 1 步：能量定义。** 取 $E(x) = \frac{x^2}{2}$（碗形能量，谷底在 0）：$p(x) = e^{-x^2/2}/Z$，其中 $Z = \int e^{-x^2/2}dx = \sqrt{2\pi} \approx 2.507$——**这一步积分在高维（百万维像素）根本不可算**，EBM 的原罪。

**第 2 步：取 log、求梯度。** $\log p(x) = -\frac{x^2}{2} - \log Z$；对 $x$ 求梯度：$\nabla_x \log p = -x$——**$Z$ 项与 $x$ 无关，求导时消失**！全程没算过 $\sqrt{2\pi}$。

**第 3 步：对照验证。** 这个 $p$ 正是标准正态：其 score 确为 $\nabla\log\mathcal{N}(x;0,1) = -x$ ✓——**能量取负梯度即 score**：$\nabla_x\log p = -\nabla_x E$，学 score 完全绕开配分函数（score matching 一族的立足点，[[40-Concepts/Score函数]]）。

**第 4 步：能算与不能算清单。** 能：score（梯度）、能量差（$E(x_1) - E(x_2) = \log\frac{p(x_1)}{p(x_2)}$，Z 消掉）；不能：绝对概率 $p(x)$（要除 Z）、归一化常数本身——**EBM 只给"相对好坏"，采样靠朗之万**（[[40-Concepts/朗之万动力学]]：有 score 就能采）。

**第 5 步：历史线读法。** 打分器（EBM，难采样）→ 只学梯度（score matching）→ 梯度可采样（朗之万）→ 连续化（SDE）——一条从统计物理直通 Score-SDE 的长征，能量是它最早的名字。

## 3. 为什么 AI 需要它

| 出现场景 | 用法 |
|---|---|
| [[40-Concepts/Score函数]] | score = 负能量梯度，score-based 生成的理论桥 |
| Boltzmann 机 / RBM | 深度学习史前史（2006 复兴的主角） |
| [[20-Algorithms/Score-Based生成模型]] | "score 网络 = 能量模型的梯度场" |
| 联合嵌入预测架构 JEPA（线 9 待建页）（线 9） | LeCun 世界模型的能量视角：预测的能量最低点 = 正确未来 |
| 联合能量模型 JEM、对比学习 InfoNCE | 判别器转生成 |

**历史脉络**：EBM（打分难采样）→ score matching（只学梯度，绕开 $Z$）→ 朗之万采样（score 可采样）→ SDE（连续时间统一）——一条从 1980s 统计物理直通 2021 Score-SDE 的长线。

## 4. 常见误区

- **误区**：能量小 = 概率小——**反了**！能量低（负得越多）概率越高（玻尔兹曼分布）
- **误区**：可以"归一化"能量网络——$Z$ 依赖 $\theta$ 且是全空间积分，训练中每步都变
- **误区**：score 学得好就能算 $p(x)$——不能，$Z$ 依然未知（只有相对概率可用）

## 5. 自测

1. $E(x) = x^2/2$ 的 $p(x)$ 与 $Z$？（标准正态；$Z = \sqrt{2\pi}$——高维不可算的原罪）
2. 梯度为什么绕开 $Z$？（$Z$ 与 $x$ 无关——$\nabla\log p = -\nabla E = -x$，全程没算积分）
3. EBM 能给与不能给什么？（能：score、能量差（相对好坏）；不能：绝对概率、归一化常数）
4. 从打分器到生成模型的桥？（score matching（只学梯度）→ 朗之万（梯度可采样）→ SDE）

## 6. 相关概念

- [[40-Concepts/Score函数]]：负能量梯度
- [[40-Concepts/概率分布]]：能量→概率的玻尔兹曼变换
- [[40-Concepts/梯度]]：实用信息全在梯度里
- 统计物理侧应用例（2026-09）：[[The Rater Ising-Potts Model with LLM-Derived Weights An Application to Multi-Category Scoring Reliab]]——Ising/Potts 自旋玻璃即最古老的能量模型，且走的是"结构已知、参数可估"路线（伪似然绕开 $Z$），与深度 EBM 的"结构可学、$Z$ 难算"恰成对照
